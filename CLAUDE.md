# CLAUDE.md

@import ./BRAIN.md

The `page-management` and `ingest` skills under `.claude/skills/` are loaded automatically — follow their rules when working on `brain/` in the matching situation. Before committing, run `node scripts/reindex.mjs`, `node scripts/lint-links.mjs`, and `node scripts/validate.mjs` in that order.
